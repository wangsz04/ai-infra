# 通用规则

## 任务分配

- 除skills 中明确要求外，**尽可能地**将任务分割为多个相关联性较低的子任务派发多个子任务并行执行(必要时也可串行), 例如内容审查，内容生成等任务。

## 输出/生成内容规则

- 除特殊内容外(代码/OpenSpec spec规范字段/特殊语法/特征词汇等外), **必须**使用**简体中文**进行回应/输出
- 文件的输出时若用户提供了路径, **必须**按照用户意图
