# 提交信息示例

## 新功能

```
feat: add user registration endpoint
feat(auth): implement JWT token validation
feat(ui): add dark mode toggle component
```

## 修复缺陷

```
fix: resolve null pointer in user service
fix(api): handle timeout on external requests
fix(db): prevent connection pool exhaustion
```

## 文档变更

```
docs: update API endpoint documentation
docs(readme): add installation instructions
```

## 代码格式

```
style: fix indentation in config files
style(lint): apply prettier formatting
```

## 重构

```
refactor: extract validation logic into shared module
refactor(auth): simplify token refresh flow
```

## 性能优化

```
perf: cache database query results
perf(search): optimize index lookup algorithm
```

## 测试

```
test: add unit tests for user service
test(auth): add integration tests for login flow
```

## 构建与 CI

```
build: upgrade webpack to v5
ci: add GitHub Actions workflow for PR checks
```

## 其他

```
chore: update dependencies
chore(deps): bump express from 4.18 to 4.19
```

## 回退

```
revert: revert feat(auth): implement JWT token validation
```

## 破坏性变更

```
feat(api)!: redesign REST API v2 endpoints

BREAKING CHANGE: API v1 endpoints are no longer supported, migrate to v2
```
